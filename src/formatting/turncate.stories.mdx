import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import truncate from "./truncate"

<Meta title="Formatting|Truncate" component={truncate}/>

# Truncate
Turncate helps deside the length of a string

```javascript
import { truncate } from "@staccx/bento"
```
```javascript
const longString = "The quick brown fox jumps over the lazy dog"
const shortString = "The quick brown fox"
```

## Standard truncate

export const longString = "The quick brown fox jumps over the lazy dog"
export const shortString = "The quick brown fox"

<Preview>
  <Story name="Standard">
   <p>{truncate(longString)}</p>
  </Story>
</Preview>

## Set turncate length 

<Preview>
  <Story name="Set length">
   <p>{truncate(longString, 10)}</p>
  </Story>
</Preview>


## Turncate with other symbol passed

<Preview>
  <Story name="Set symbol">
   <p>{truncate(longString, 10, "---")}</p>
  </Story>
</Preview>
