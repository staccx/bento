import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks"
import {useState} from "react"
import ReactJson from "react-json-view"
import List from "../../components/Lists/List"
import useBrRegSearch from "./useBrRegSearch"
import Input from "../../components/Forms/Input/Input"

<Meta
  title="HOOKS|UseBrRegSearch"
  component={useBrRegSearch}
/>

# useBrRegSearch

### Import
All Hooks are imported via hooks. to use a spesific hook you would
use ```hooks.hookYouWant```

```javascript
import { hooks } from "@staccx/bento"
```
usage
```javascript
hooks.useBrRegSearch
```


## useBrRegSearch setup component

```javascript
import {hooks, List} from "@stacc/bento"
import ReactJson from "react-json-view"
import React from "react"

export const Component = ({ term }) => {
  const { isLoading, results } = hooks.useBrRegSearch(term)

  return (
    <div>
      <List>
        {isLoading
          ? "Loading..."
          : results.map(item => (
              <ReactJson
                key={item.organisasjonsnummer}
                src={item}
                name={`${item}`}
                theme="monokai"
                collapsed
              />
            ))}
      </List>
    </div>
  )
}
```

## Standard useBrRegSearch

<Preview>
  <Story name="Standard">
    {() => {
      const [search, setSearch] = useState("")
      const { isLoading, results } = useBrRegSearch(search)
    return <div>
    <Input onChange={e => setSearch(e.target.value)} placeholder="Search here" style={{width:300}}/>
      <List>
        {isLoading
          ? "Loading..."
          : results.map(item => (
          <ReactJson
            key={item.organisasjonsnummer}
            src={item}
            name={`${item}`}
            theme="monokai"
            collapsed
          />
          ))}
      </List>
    </div>
    }}
  </Story>
</Preview>
