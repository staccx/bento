import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Logo from "./Logo"

<Meta title="components/Logo" component={Logo}/>

# Logo
Forklaring på hva det er og hvordan den kan brukes

```javascript
import { Logo } from "@staccx/bento"
```

## Standard Logo
This is the standard Logo

<Preview>
  <Story name="Standard">
   <Logo brand={"dnb"} />
  </Story>
</Preview>

## Flere stories her
Hva er spesielt med denne storien?

### Source code
<details><summary>Logo</summary>
<p>

```bash
import PropTypes from "prop-types"
import React from "react"

const Logo = ({ brand, height, width, className }) => (
  <img
    height={height}
    width={width}
    src={`https://api.logos.staccsandbox.com/${brand}`}
    alt={brand}
    className={className}
  />
)

export default Logo

Logo.propTypes = {
  brand: PropTypes.string.isRequired,
  height: PropTypes.number,
  width: PropTypes.number
}

Logo.defaultProps = {
  height: 48
}

```

</p>
</details>

## Props for Logo
<Props of={Logo} />
