import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import CodeDevice from "./CodeDevice"

<Meta title="Components|Icons/CodeDevice" component={CodeDevice}/>


# CodeDevice
This is a icon called CodeDevice. The code device is used to signalize that the signing method needs a code device to sign or authenticate.


```javascript
import { CodeDevice } from "@staccx/bento"
```

## Standard CodeDevice
This is the standard CodeDevice.

<Preview>
  <Story name="Standard">
   <CodeDevice/>
  </Story>
</Preview>


## Props for CodeDevice
<Props of={CodeDevice} />
