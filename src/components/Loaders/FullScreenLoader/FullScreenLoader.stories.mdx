import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import FullScreenLoader from "./FullScreenLoader"
import {messages} from "../loaders.messages"


<Meta title="Components|Loaders/FullScreenLoader" component={FullScreenLoader}/>

# FullScreenLoader
FullScreenLoader is a LoadingComponent great for transitions when also doing something Async, like
API requests.

```javascript
import { FullScreenLoader } from "@staccx/bento"
```

## Standard FullScreenLoader
This is the standard FullScreenLoader

  <Story name="Standard">
  <div>
   <FullScreenLoader messages={messages} random/>
   </div>
  </Story>


## Props for FullScreenLoader
<Props of={FullScreenLoader} />
