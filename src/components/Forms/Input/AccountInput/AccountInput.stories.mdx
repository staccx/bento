import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import AccountInput from "./AccountInput"

<Meta title="Components|Forms/Input/AccountInput" component={AccountInput}/>


# AccountInput
This components controlls user input so that they can only enter numbers with the format of an accountNumber



```javascript
import {AccountInput} from "@staccx/bento"
```


## Standard AccountInput
The inputfield only allowed numbers and takes number in a 4-2-5 format with seperators between and max 11 carrecters


<Preview>
  <Story name="Standard">
   <AccountInput label={"AccountInput"} id="9852145" />
  </Story>
</Preview>





## Props for AccountInput
<Props of={AccountInput} />
