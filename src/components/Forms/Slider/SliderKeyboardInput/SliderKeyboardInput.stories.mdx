import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import SliderKeyboardInput from "./SliderKeyboardInput"

<Meta title="Components|Forms/SliderKeyboardInput" component={SliderKeyboardInput}/>


# Slider


```javascript
import {SliderKeyboardInput} from "@staccx/bento"
```

## Example usage
<Preview>
  <Story name="Default">
    <SliderKeyboardInput label="Slider with keyboard input" min={100} max={500} step={50} defaultValue={250} onChange={(value) => console.log("The value in default is ",value)}  />
  </Story>
</Preview>

<Preview>
  <Story name="Default value outside of step increment">
    <SliderKeyboardInput name="stepoutside" label="Slider with keyboard input" min={100} max={500} step={50} defaultValue={251} onChange={(value) => console.log("The value step e is ",value)}  />
  </Story>
</Preview>

## Props for SliderKeyboardInput
<Props of={SliderKeyboardInput} />
