import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import SliderKeyboardInput from "./SliderKeyboardInput"

<Meta title="Components|Forms/SliderKeyboardInput" component={SliderKeyboardInput}/>


# Slider


```javascript
import {SliderKeyboardInput} from "@staccx/bento"
```

## Example usage
<Preview>
  <Story name="Default">
    <SliderKeyboardInput label="Slider with keyboard input" min={100} max={500} step={50} defaultValue={250}  />
  </Story>
</Preview>

<Preview>
  <Story name="Default value outside of step increment">
    <SliderKeyboardInput name="stepoutside" label="Default value outside of step increment" min={100} max={500} step={50} defaultValue={251}  />
  </Story>
</Preview>

<Preview>
  <Story name="Big un">
    <SliderKeyboardInput name="big" label="Large range, low step" min={10} max={100000} step={10} defaultValue={65356}  />
  </Story>
</Preview>

## Props for SliderKeyboardInput
<Props of={SliderKeyboardInput} />
