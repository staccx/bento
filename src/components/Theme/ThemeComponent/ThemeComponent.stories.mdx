import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import ThemeComponent from "./ThemeComponent"

<Meta title="components/Theme/ThemeComponent" component={ThemeComponent}/>

# ThemeComponent
When you need a component inside the theme. E.g a logo that changes between themes or whatever you need

```javascript
import { ThemeComponent } from "@staccx/bento"
```

## Standard Theme


<Preview>
  <Story name="Default">
  <>
  <div>
  Change theme to see the component change
  </div>
  <br/>
   <ThemeComponent tagName="logo" fallback={() => <div>This theme has no logo</div>} />
   </>
  </Story>
</Preview>

## Props for WidowFix
<Props of={ThemeComponent} />
