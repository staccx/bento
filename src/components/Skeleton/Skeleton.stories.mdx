import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import SkeletonCircle from "./Skeleton.Circle"
import SkeletonLine from "./Skeleton.Line"
import SkeletonPulse from "./Skeleton.Pulse"

<Meta title="Components|Skeleton" component={SkeletonCircle}/>

# Skeleton
Skeleton is used as a placeholder and a illustrator that the page is loading components.
It is suppost to take the shape of the loading component and render a skeleton of that component,
also with a pulsing effect to illustrate loading.

```javascript
import { SkeletonCircle } from "@staccx/bento"
```

## Standard Skeleton
The Skeleton Component are three components, SkeletonCircle, SkeletonLine nad SkeletonPulse

<Preview>
  <Story name="Standard">
  <>
       <SkeletonCircle style={{ padding: 40 }}/>
       <SkeletonLine style={{ paddingLeft: 40, marginLeft: 5}}/>
       <SkeletonPulse />
   </>
  </Story>
</Preview>

## Props for Skeleton
<Props of={SkeletonCircle} />
<Props of={SkeletonLine} />
<Props of={SkeletonPulse} />
