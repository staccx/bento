import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import {colorNames} from './colorNames';

<Meta title="Styling|/Colors" component={colorNames}/>

# ColorNames
All the colors used in Bento!

```javascript
import { colorNames } from "@staccx/bento"
```

## All colorNames
All Colors

export let colors = Object.keys(colorNames).map(function(key){
return <div key={key} style={{marginRight:5, border:1, borderColor:"black", borderStyle:"solid", marginBottom:20, height:150, width:150, textAlign:"center", backgroundColor: colorNames[key]}}><p style={{marginTop: 150}}>{key}</p></div> });


   <Story name="test">
    <div style={{display:"flex",flexDirection:"row",flexWrap:"wrap"}}>
    {colors}
    </div>
  </Story>
