import React from "react"
import {{name}} from "./{{name}}"

const preview = {
  title: "{{name}}",
  category: "component",
  tags: [],
  component: {{name}},
  render: props => <{{name}} {...props}>Cool {{name}}</{{name}}>
}

export default preview
