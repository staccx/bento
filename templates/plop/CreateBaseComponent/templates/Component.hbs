import React from "react"
import PropTypes from "prop-types"
import styled, { css } from "styled-components"

import {
  spacing,
  themify,
  ThemeComponent,
  color,
  borderRadius,
  themeProps
} from "@staccx/theme"
import themePropTypes from "../constants/themePropTypes"

const tProps = {
  
}

const {{name}} = ({ children, type, ...restProps }) => {
  return (
    <div />
  )
}

{{name}}.propTypes = {}

{{name}}.defaultProps = {}

{{name}}.themeProps = tProps

export default {{name}}
