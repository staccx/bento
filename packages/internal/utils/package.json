{
  "name": "@staccx/utils",
  "version": "5.1.0",
  "description": "Utilites",
  "author": "StaccX",
  "license": "UNLICENCED",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@bitbucket.org/stacc-flow/bento.git"
  },
  "main": "dist/index.js",
  "module": "dist/index.es.js",
  "jsnext:main": "dist/index.es.js",
  "scripts": {
    "test": "react-scripts test --env=jsdom --coverage",
    "test:watch": "react-scripts test --env=jsdom",
    "build": "yarn prettify && yarn test && rollup -c && node build.js",
    "start": "rollup -c -w",
    "prepublish": "yarn build",
    "prettify": "prettier --write '{src,__{tests,mocks}__}/**/*.js'"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "https://stacc.jfrog.io/stacc/api/npm/npm/"
  },
  "dependencies": {
    "@staccx/soy": "^5.1.0"
  }
}
