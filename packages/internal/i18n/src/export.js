import { default as formatCurrency } from "./plugins/format.currency"
export { default as translate } from "./translate"
export { default as convert } from "./convert"

export const plugins = {
  formatCurrency
}
