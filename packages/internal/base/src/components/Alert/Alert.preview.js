import React from "react"
import Alert from "./Alert"

const preview = {
  title: "Alert",
  category: "Components",
  component: Alert,
  render: props => <Alert {...props} />
}

export default preview
