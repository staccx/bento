import React from "react"
import Wrapper from "./Wrapper"

const preview = {
  title: "Wrapper",
  category: "component",
  component: Wrapper,
  render: props => <Wrapper {...props} />
}

export default preview
