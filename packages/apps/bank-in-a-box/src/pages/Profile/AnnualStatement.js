import React from "react"
import { Layout, Wrapper, Heading } from "@staccx/base"

const AnnualStatement = ({ history, accounts }) => (
  <Wrapper variant="bib">
    <Layout>
      <Heading>Årsoppgave</Heading>
    </Layout>
  </Wrapper>
)

export default AnnualStatement
