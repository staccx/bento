{
  "name": "@staccx/savings-advisor",
  "version": "6.0.0",
  "private": true,
  "dependencies": {
    "@staccx/animations": "^6.0.0",
    "@staccx/base": "^6.0.0",
    "@staccx/formatting": "^6.0.0",
    "@staccx/math": "^6.0.0",
    "@staccx/soy": "^6.0.0",
    "@staccx/theme": "^6.0.0"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test --env=jsdom",
    "eject": "react-app-rewired eject",
    "deploy": "yarn build && surge -p ./build -d savings-advisor.surge.sh",
    "serve": "yarn build && serve -s build"
  },
  "devDependencies": {
    "@staccx/eslint-config": "^5.1.0"
  },
  "proxy": "https://demo.quantfol.io/"
}
