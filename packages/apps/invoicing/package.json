{
  "name": "@staccx/invoicing",
  "version": "9.0.0-alpha.39",
  "private": true,
  "main": "dist/index.js",
  "module": "dist/index.es.js",
  "jsnext:main": "dist/index.es.js",
  "dependencies": {
    "@staccx/base": "^9.0.0-alpha.39",
    "mobx-react": "^5.2.5",
    "prop-types": "^15.6.2",
    "react-router-dom": "^4.2.2"
  },
  "scripts": {
    "build": "yarn prettify && rollup -c",
    "start": "react-scripts start",
    "build:app": "react-scripts build",
    "test": "react-scripts test --coverage --watchAll=false --passWithNoTests",
    "eject": "react-scripts eject",
    "deploy": "yarn build && surge -p ./build",
    "serve": "yarn build && serve -s build",
    "prepare": "yarn build",
    "prepublishOnly": "yarn test",
    "prettify": "prettier --write '{src,__{tests,mocks}__}/**/*.js'"
  },
  "devDependencies": {
    "@staccx/rollup-config": "^9.0.0-alpha.39",
    "serve": "^10.0.0",
    "surge": "^0.20.1"
  }
}
